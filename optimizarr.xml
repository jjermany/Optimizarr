<?xml version="1.0"?>
<Container version="2">
  <Name>Optimizarr</Name>
  <Repository>ghcr.io/jjermany/optimizarr:latest</Repository>
  <Registry>https://ghcr.io</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>bash</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/jjermany/Optimizarr/issues</Support>
  <Project>https://github.com/jjermany/Optimizarr</Project>
  <Overview>Automated media library optimizer with Intel QSV / VAAPI hardware-accelerated transcoding. Monitors your media libraries, queues encoding jobs, and replaces source files with smaller, re-encoded versions — all from a single container.</Overview>
  <Category>MediaApp:</Category>
  <WebUI>http://[IP]:[PORT:8080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/jjermany/Optimizarr/main/optimizarr.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/jjermany/Optimizarr/main/icon.png</Icon>
  <ExtraParams>--device=/dev/dri:/dev/dri</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Description>Automated media library optimizer with Intel QSV / VAAPI hardware-accelerated transcoding. Monitors your media libraries, queues encoding jobs, and replaces source files with smaller, re-encoded versions — all from a single container.</Description>

  <!-- Paths -->
  <Config Name="Media" Target="/media" Default="/mnt/user/data/media" Mode="rw"
    Description="Path to your media libraries."
    Type="Path" Display="always" Required="true" Mask="false">/mnt/user/data/media</Config>

  <Config Name="Config" Target="/config" Default="/mnt/cache/appdata/optimizarr" Mode="rw"
    Description="Persistent configuration and database storage."
    Type="Path" Display="always" Required="true" Mask="false">/mnt/cache/appdata/optimizarr</Config>

  <Config Name="Cache" Target="/cache" Default="/mnt/cache/appdata/optimizarr/tmp" Mode="rw"
    Description="Temporary workspace used during encoding. Place on a fast drive."
    Type="Path" Display="always" Required="true" Mask="false">/mnt/cache/appdata/optimizarr/tmp</Config>

  <!-- Port -->
  <Config Name="Web UI Port" Target="8080" Default="8080" Mode="tcp"
    Description="Port the web UI is accessible on."
    Type="Port" Display="always" Required="true" Mask="false">8080</Config>

  <!-- Environment variables -->
  <Config Name="VA-API Driver" Target="LIBVA_DRIVER_NAME" Default="iHD" Mode=""
    Description="VA-API driver name. Use iHD for Intel Gen 8+ / Arc (recommended) or i965 for older hardware."
    Type="Variable" Display="always" Required="false" Mask="false">iHD</Config>

  <Config Name="QSV Device" Target="QSV_DEVICE" Default="/dev/dri/renderD128" Mode=""
    Description="Path to the Intel render device used for QSV hardware encoding."
    Type="Variable" Display="always" Required="false" Mask="false">/dev/dri/renderD128</Config>

  <Config Name="UI Username" Target="OPTIMIZARR_UI_USERNAME" Default="" Mode=""
    Description="Optional: username for basic auth on the web UI. Leave blank to disable auth."
    Type="Variable" Display="always" Required="false" Mask="false"></Config>

  <Config Name="UI Password" Target="OPTIMIZARR_UI_PASSWORD" Default="" Mode=""
    Description="Optional: password for basic auth on the web UI. Leave blank to disable auth."
    Type="Variable" Display="always" Required="false" Mask="true"></Config>
</Container>
