services:
  plex-optimizer:
    build:
      context: .
    container_name: plex-optimizer
    ports:
      - "8080:8080"
    volumes:
      - ./media:/media
      - ./cache:/cache
      - ./config:/config
      - /dev/dri:/dev/dri
    restart: unless-stopped
